package validations;

public class Valid {
	public boolean isValid;
	public String message;
	
	public Valid(boolean isValid, String message) {
		this.isValid = isValid;
		this.message = message;
	}
}
